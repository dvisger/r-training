#create function 
# name the function, call function, name arguments, contents of argument with named arguments

plus_one <- function(x) {
  x+1
}

#run with input
plus_one(100)

#run with object
plus_one(num)

#evaluates with logical
plus_one(lg)

#does not evaluate with character
plus_one(ch)

#print contents
plus_one

is.function(plus_one)
